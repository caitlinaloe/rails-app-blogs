<!--
  Users need a form that allows them to create a new blog.
  The form doesn't actaully need to work.
  The import thing is that html form fields appear on the page.
  https://www.w3schools.com/html/html_forms.asp
-->
</div class="fillP">
  <div class="formbox">
    <% if @blog.errors.any? %>
      <h1>Please correct the following issues: </h1>
      <%= @blog.errors.messages do |key, value | %>
        <p>"#{key}: #{value}"</p>
      <% end %>
    <% end %>
    <%= form_for @blog do |form| %>
      Title: <%= form.text_field :title %><br />
      Content: <%= form.text_field :content %><br />
      <%= form.submit %>
    <% end %>
  </div>
</div>
